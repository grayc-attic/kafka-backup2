apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kafka-backup-consumer
spec:
  rules:
    - host: kafka-backup-consumer-management.127.0.0.1.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kafka-backup-consumer
                port:
                  name: management
